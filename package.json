{
  "name": "@tspower/imageloader",
  "version": "1.5.0",
  "description": "funny image loader component",
  "main": "./lib/imageloader.js",
  "types": "./lib/imageloader.d.ts",
  "directories": {
    "doc": "docs",
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "setup": "npm install",
    "build": "tsc",
    "build-watch": "tsc --watch",
    "test-only": "mocha --require ts-node/register test/**/*.ts",
    "pretest": "npm run build",
    "test": "npm run test-only",
    "typedoc": "typedoc --out ./docs --mode modules --tsconfig ./tsconfig.json "
  },
  "keywords": [],
  "author": "Luigi Senzamici (luigisenzamici@gmail.com)",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.1.6",
    "@types/mocha": "^5.2.5",
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "ts-node": "^7.0.1",
    "typedoc": "^0.13.0"
  },
  "dependencies": {
    "@tspower/core": "^1.5.0",
    "@tspower/fileloader": "^1.0.0",
    "@tspower/http": "^1.1.0",
    "@tspower/spinner": "^1.0.0",
    "rxjs": "^6.3.3"
  }
}
